# Scala Newtype Example

Check on how well the Scala newtype works, package comes from:
<https://github.com/estatico/scala-newtype>.

## How to run

You will need `sbt`. Simply run

```bash
sbt run
```
